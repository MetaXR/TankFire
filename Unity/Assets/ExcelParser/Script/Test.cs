using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*
		DBEnterDeviceInfoMgr.instance.InitData ();

		DBEnterDeviceInfoBean ed = DBEnterDeviceInfoMgr.instance.GetDataById (1);
		Debug.Log ("ID text ==>>" + ed.Id + " "+ ed.Layer);

		for (int i = 5; i < 10; i++) {
			DBEnterDeviceInfoBean edi = DBEnterDeviceInfoMgr.instance.GetDataByRow (i);
			Debug.Log ("Row text ==>>" + edi.Id + " "+ edi.Layer);
		}
		*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
